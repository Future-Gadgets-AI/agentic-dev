# macOS
.DS_Store

# Machine-local Claude Code settings
.claude/settings.local.json
.claude/*.local.md

# Ephemeral handoff / scratch (never committed)
local_files/

# ADR / issue drafts — ephemeral; published as GitHub issues, not committed
notes/
